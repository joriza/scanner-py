{
    "project": "Scanner Pro Python",
    "version": "1.2.0",
    "core_rules": {
        "sorting": "Hierarchy: Score (D+W active) > Weekly Days (ASC) > Daily Days (ASC).",
        "weekly_logic": "Always resample Daily -> Weekly using frequency 'W-FRI'. Cap dates to today.",
        "macd_logic": "Standard (12, 26, 9). Color RED if MACD > 0 OR MACD < Signal.",
        "rsi_logic": "Period 14. Rebound signal only counts AFTER the most recent oversold (<30) event.",
        "ui_aesthetics": "Premium Dark Mode, Glassmorphism, dynamic table headers based on strategy_id."
    },
    "database": "SQLite (database.py). Incremental sync via FinanceService.sync_ticker_data.",
    "api_standard": "All technical signals computed in memory via FinanceService.get_signals(ticker, strategy)."
}